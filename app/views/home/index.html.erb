<%= render "home/header" %>

<div class="row">    
    <% @decklists.each_with_index do |decklist, i| %>
        <div class="col-md-6">
            <%= render "home/card", decklist: decklist %>
        </div>
    <% end %>
</div>

