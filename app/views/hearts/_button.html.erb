<% if current_user.heart?(decklist) %>
  <%= button_to "Unheart", unheart_path(decklist_id: decklist.id), remote: true, method: :delete  %>
<% else %>
  <%= button_to "Heart", heart_path(decklist_id: decklist.id), remote: true  %>
<% end %>