<div class="row">
    <div class="col">
        <h4>Tags</h4>
        <% if card.tag_list.length > 0 %>
            <% card.tag_list.each do |t| %>
                <%= link_to t, card_tag_path(t) %>
                
            <% end %>
        <% else %>
            No Tags Exist!
        <% end %>

        <% if current_user %>
            <%= form_tag(edit_card_path(card), method: "GET") do %>
                <div><%= submit_tag 'Add Tags', class: "btn btn-sm btn-primary" %></div>
            <% end %>
        <% end %>
    </div>
</div>